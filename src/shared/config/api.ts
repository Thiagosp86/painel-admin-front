export const API_URL = 'http://localhost:3000'; // ajuste se necessário
export const API_VERSION = 'v1'; // versão da API, ajuste conforme necessário
export const API_BASE_URL = `${API_URL}/${API_VERSION}`; // URL base da API
